@model List<ChatUserResponseDto>

<div class="container h-100">
    <div class="row">
        <div class="col-4">
            @foreach(var user in Model)
            {
                <div class="w-100" data-user="@user.UserId">
                    @user.UserName
                </div>
            }
        </div>
        <div class="col-8">
            
        </div>
    </div>
</div>